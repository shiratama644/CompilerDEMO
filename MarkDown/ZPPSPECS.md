### Z++  

### 1 (Introduction)

#### 1.1. Z++
Z++**8CPU3**C++

Z++ **`.zpp`** 

#### 1.2. 
Z++3

*   ** (Transparency):** Z++
*   ** (Efficiency):** CPU
*   ** (Expressiveness):** I/O

### 2 (Lexical Structure)

#### 2.1.  (Comments)


*   **:** `//` 
    ```zpp
    int led_pin = 3; // LED
    ```
*   **:** `/*`  `*/` 
    ```zpp
    /*
      
      
    */
    ```

#### 2.2.  (Identifiers)

*   **:**  (`a-z`, `A-Z`)  (`_`) 2
*   **:** `myValue`  `myvalue` 
*   **:** `counter`, `_status`, `ADC_Read_Value`

#### 2.3.  (Keywords)

`int`, `void`, `const`, `if`, `else`, `while`, `do`, `for`, `return`, `switch`, `case`, `default`, `break`, `Output`, `Input`, `Run`, `struct`, `class`, `public`, `private`, `sizeof`

#### 2.4.  (Literals)

*   **10:** `123`
*   **16:** `0xFF` `0x`
*   **2:** `0b10101010` `0b`

### 3 (Data Types, Variables, and Constants)

#### 3.1. 
Z++

*   **`int`:** **8** (`0`  `255`) 
*   **`void`:** `void`
*   **`struct`:** 3.4
*   **`class`:** 3.5
*   **:** 3.6

#### 3.2.  (Variables)

*   **:** `int ;` 
*   **:**  `int life = 3;`
*   ** ():**
    *   **:** `{}` 
    *   **:** 

#### 3.3.  (`const`)

*   **:** ********
*   **:** `const int  = ;` 
*   **:** ****
*   **:** `const` 
*   **:**
    ```zpp
    const int LED_PIN = 3;
    const int SENSOR_PORT = 2;
    Output(255, LED_PIN); // 3LED
    ```

#### 3.4.  (struct) - 

`struct`Z++`struct``class`

##### 3.4.1. `class`
`struct``class`2

*   **`struct`**
    `struct`******public******

*   **`class`**
    `class`******private******

##### 3.4.2. `class`
Z++`struct``class`** (`public`, `private`) **

*   **`struct`:** **`public`**
    ```zpp
    struct Point {
        int x; //  public
    };
    ```
     `struct Point { public: int x; };` 

*   **`class`:** **`private`**
    ```zpp
    class Player {
        int hp; //  private
    };
    ```
     `class Player { private: int hp; };` 

`struct``private:``class``public:`

##### 3.4.3. 
`struct``class`

*   **:** 
*   **:** `class``this``r1`
*   **:** `struct`

##### 3.4.4. `sizeof`
`struct`

##### 3.4.5. 
`Point``public`

```zpp
// 2 'Point' 
struct Point {
    //  public:
    int x;
    int y;

    // (0, 0)
    Point() {
        x = 0;
        y = 0;
    }

    // 
    void move(int dx, int dy) {
        x += dx;
        y += dy;
    }

    // 2
    // (2)
    int distance_sq_from_origin() {
        // 
        int x_sq_low = x * x;
        int x_sq_high = x ** x;
        int y_sq_low = y * y;
        int y_sq_high = y ** y;

        // 168
        // 
        return x_sq_low + y_sq_low;
    }
};

// --- struct ---
int main() {
    // p1(0, 0)
    struct Point p1;

    // public
    p1.x = 10;
    p1.y = 20;

    // 
    p1.move(-5, 10); // p1 (5, 30) 

    int dist_sq = p1.distance_sq_from_origin();

    return 0;
}
```

#### 3.5.  (class) - 

`class`

##### 3.5.1. `struct`
`struct``struct Player { int hp; };`  `player.hp = 999;` 

`class``class`

##### 3.5.2. `public`/`private`
`class``public``private`2

*   **`public` ():**
    (API)

*   **`private` ():**
    `private`****

*   **:**
    `public`/`private`Z++********

##### 3.5.3. 
*   **:** `private`
*   **:** 
    1.  **:** `class` `{}` 
    2.  **:** `class`** `::`** 

##### 3.5.4.  (Constructor)
****

*   **:** 
*   **:** 
*   **:** 

##### 3.5.5. `this`
`class`: `player1`, `player2``hp`RAMROM

`player1.apply_damage(10)``player2.apply_damage(10)``hp`**`this`**

*   **:**
    ```zpp
    player1.apply_damage(10);
    ```
*   **:**
    `apply_damage``player1`****`10`****`this`

*   **:**
    `this`**`r1`**`r2`, `r3`...

##### 3.5.6. 
Z++`class`CPUC++
*   ****
*   ** (Virtual Functions)**
*   ** (Destructor)**

`class`

##### 3.5.7. 
```zpp
// ---  ---
class Timer {
public:
    Timer(); // 
    void set_limit(int new_limit);
    void tick();
    int has_expired();

private:
    int counter;
    int limit;
    int is_expired;
};


// ---  () ---

// 
Timer::Timer() {
    counter = 0;
    limit = 255;
    is_expired = 0;
}

// 
void Timer::set_limit(int new_limit) {
    limit = new_limit;
}

// 1
void Timer::tick() {
    counter = counter + 1;
    if (counter >= limit) {
        is_expired = 1;
        counter = 0; // 
    }
}

// 
int Timer::has_expired() {
    if (is_expired == 1) {
        is_expired = 0; // 
        return 1;
    }
    return 0;
}


// --- Z++ ---

int main() {
    class Timer short_timer; // 
    class Timer long_timer;

    short_timer.set_limit(100);
    long_timer.set_limit(200);

    // 255
    for (int i = 0; i < 255; i++) {
        short_timer.tick();
        long_timer.tick();

        if (short_timer.has_expired()) {
            //  (: 01)
            Output(1, 0);
        }

        if (long_timer.has_expired()) {
            //  (: 11)
            Output(1, 1);
        }
    }

    return 0;
}
```

#### 3.6.  (array)



*   **:** ` [];` ``
    ```zpp
    int scores[10];       // int10
    struct Point path[5]; // struct Point10
    ```

*   **:**  `{}` 
    ```zpp
    int pins[4] = {2, 3, 4, 5};
    int data[5] = {10, 20}; // {10, 20, 0, 0, 0} 
    ```

*   **:**  `[]` `0`
    ```zpp
    scores[0] = 100;
    int first_score = scores[0];
    ```
    **** 

*   **:** 
    ```zpp
    int matrix[3][4]; // 342
    matrix[1][2] = 1; // 23
    ```

*   **`sizeof`:** `sizeof`
    ```zpp
    int scores[10];
    int total_size = sizeof(scores); // 10  (sizeof(int) * 10)
    ```

*   **:** ** `&` **7.2
    ```zpp
    void process_data(int arr[]);
    process_data(&scores); // 
    ```

### 4 (Expressions and Operators)

#### 4.1. 
*   `+` (), `-` (): : `255 + 2`  `1` 
*   `*` ( - ): 8**8**()
*   `**` ( - ): 8**8**()
*   `/` (): ()
*   `%` (): ()
*   `++` (), `--` ()
*   `+=`, `-=`, `*=`, `**=`, `/=`, `%=` ()

**:**
```zpp
int a = 10;
int b = 3;
int product_low = a * b; // product_low  30
int product_high = a ** b // product_high  0;
int quotient = a / b; // quotient  3
int remainder = a % b; // remainder  1
```

#### 4.2. 
`==` (), `!=` (), `<` (), `>` (), `<=` (), `>=` ()

#### 4.3. 
*   `&&` (AND), `||` (OR): ****

#### 4.4. 
`&` (AND), `|` (OR), `^` (XOR), `~` (NOT), `<<` (), `>>` () 

#### 4.5.  (`&`)


#### 4.6. 
*   **`.` ():** 
    ```zpp
    struct Point p;
    p.x = 10; // px
    ```
*   **`->` ():** `(*p).x` 
*   **`::` ():** `::` 
*   **`sizeof` ():** 
    ```zpp
    int size_of_int = sizeof(int);           // 1
    int size_of_point = sizeof(struct Point); // 2 (int + int)
    ```

#### 4.7. 
*   **`[]` ():** 

### 5


1.  ** 1 ():** `()`, `[]`, `.`, `->` (, , ) / 
2.  ** 2:** `++`, `--` () / 
3.  ** 3:** `**` ( - ) / 
4.  ** 4:** `++`, `--`, `~`, `&` (), `sizeof` () / ****
5.  ** 5:** `*`, `/`, `%` () / 
6.  ** 6:** `+`, `-` () / 
7.  ** 7:** `<<`, `>>` () / 
8.  ** 8:** `<`, `>`, `<=`, `>=` () / 
9.  ** 9:** `==`, `!=` () / 
10. ** 10:** `&` (AND) / 
11. ** 11:** `^` (XOR) / 
12. ** 12:** `|` (OR) / 
13. ** 13:** `&&` (AND) / 
14. ** 14:** `||` (OR) / 
15. ** 15 ():** `=`, `+=`, `-=`, `*=`, `**=`, `/=`, `%=`, `&=`, `|=`, `^=`, `<<=`, `>>=` () / ****

** `::` :**
*   `::` `class MotorDriver::MotorDriver()` 
*    `::` 

### 6 (Statements and Control Structures)

#### 6.1. `if - else if - else` 

```zpp
if (temperature > 100) {
    // : ON
} else if (temperature > 60) {
    // : LED
} else {
    // 
}
```

#### 6.2. `while`  (`while (condition) { ... }`)


#### 6.3. `do-while`  (`do { ... } while (condition);`)


#### 6.4. `for`  (`for (init; cond; post) { ... }`)

```zpp
// 0910
for (int i = 0; i < 10; i++) {
    // 
}
```

#### 6.5. `switch` 

*   `break``case`****
```zpp
switch (mode) {
    case 0:
        // 
        break;
    case 1:
        // 
        break;
    default:
        // 
        // 
        break;
}
```

#### 6.6. `break`  (`break;`)
 `while`, `do-while`, `for`, `switch` 

#### 6.7. `return`  (`return;`  `return expression;`)
`expression``int``int`

### 7 (Functions)

#### 7.1. 

```zpp
// 2
int add(int a, int b) {
    return a + b;
}

// 
int result = add(10, 20); // result30
```

##### 7.1.1. 


*   **:** ****`=`
    ```zpp
    void set_led(int pin, int brightness = 255);
    ```
*   **:** 
    ```zpp
    set_led(3);       // pin=3, brightness=255 
    set_led(3, 128);  // pin=3, brightness=128 
    ```
*   **:** `void func(int a = 10, int b);` 
*   **:** 

#### 7.2.  (Calling Convention)


*   **:**
    *   ** ():** `int`, `struct`, `class`****
    *   ** (`&`):**  `&` 8
        ```zpp
        // : p1
        void draw_point(struct Point pt);
        draw_point(p1);

        // : p1
        void move_point(struct Point& pt);
        move_point(p1);
        ```

*   ** ():**
    *   **`&`** 
    *   
    *    `&` 
        ```zpp
        void process_scores(int arr[]); // 

        int my_scores[50];
        process_scores(&my_scores); // 
        // process_scores(my_scores); // 
        ```

*   **:**
    *   **:** `int` (`&`) 
        *   1:  `r1` (`this`)
        *   2:  `r2`
        *   3:  `r3`
        *   4:  `r4`
    *   **:**
        *   5`int`
        *   **`struct``class`** RAM

*   **:**
    *   `int`  `r15` 
    *   : `struct``class`

*   **:**
    *   **Caller-Saved ():** `r1-r4`, `r15`
    *   **Callee-Saved ():** `r5-r14`

### 8 (Built-in Features)

#### 8.1. I/O (`cout`, `cin`)

*   **`cout(port) << value1 << value2 << ... << end;`**
    
    *   `port``int`
    *   `<<` `int`
    *   `end``;`

*   **`cin(port) >> var1 >> var2 >> ... >> end;`**
    
    *   `port``int`
    *   `>>` ****
    *   `end``;`

**:**
```zpp
int sensor_val;
const int LED_PORT = 3;

// 2sensor_val
cin(2) >> sensor_val >> end;

// LED_PORT(3)sensor_val255
cout(LED_PORT) << sensor_val << 255 << end;
```

#### 8.2. 
*   **`Run.Asm("");`**: 
*   **`Run.AsmBlock { ... }`**: 
*   **** CPU

#### 8.3.  (`Run.Debug`)

`Run.Debug`**IDE**

*   **:**
    `Run.Debug("", expression1, expression2, ...);`

*   **:**
    Z++********

    *   ** (`.zpp` -> `.asm`):**
        Z++`Run.Debug`****

    *   ** (IDE):**
        Z++`expression``{}`IDE

*   **:**
    *   ****
    *   **`{}`******

*   **:**
    ```zpp
    struct Point p;
    p.x = 50;
    p.y = 100;

    // IDE "Player position: (50, 100)" 
    Run.Debug("Player position: ({}, {})", p.x, p.y);

    // IDE "Initialization complete." 
    Run.Debug("Initialization complete.");
    ```

### 9 (Modules and Namespaces)

Z++C/C++

`#include`  `using namespace` 

#### 9.1. 
Z++2

*   **:** `#include` 
*   **:** `using namespace` 

#### 9.2.  (`#include`)

`#include`

*   **:** `#include ""`
    *   **:** Z++****

*   **:**
    1.  ****
    2.  : `math.zpp`****
    3.  `struct`/`class``const`****

#### 9.3. 

##### 9.3.1. 
`#include`****

*   **:** `using namespace``#include`
*   **:** 

##### 9.3.2.  (`using namespace`)
`using namespace`********

*   **:** `using namespace 1, 2, ...;`

*   **:**
    *   `using namespace`****
    *   `using namespace`****
    *   

*   **:**
    1.  **:** : 
    2.  **:** 

#### 9.4. 


1.  **1**
    *   `#include`

2.  **2**
    *   `using namespace`
    *   **`using namespace`:** 
    *   **`using namespace`:** `#include`

3.  **3**
    *   `using namespace`
    *   
    *   ****ROM

#### 9.5. 

**`my_math.zpp`**
```zpp
//  add  sub 2
int add(int a, int b) { return a + b; }
int sub(int a, int b) { return a - b; }
```

**1`using namespace` **
```zpp
// main_full_import.zpp

#include "my_math.zpp"
// using namespace 

int main() {
    int result1 = 0;
    int result2 = 0;

    // my_math.zpp 
    // add  sub 
    result1 = add(10, 5); // OK
    result2 = sub(10, 5); // OK

    return result1 + result2;
}
```

**2`using namespace` **
```zpp
// main_selective_import.zpp

#include "my_math.zpp"

// using namespace  'add' 
using namespace add;

int main() {
    int result = 0;

    // 'add' 
    result = add(10, 5); // OK

    // 'sub' 
    // result = sub(10, 5); // Error: 'sub' is not defined in this scope.

    return result;
}
```